"""Traffic Simulation Module.

This package provides tools for processing and analyzing traffic simulation data,
including XML to Parquet conversion, animation generation, and heatmap visualization.

Main components:
    - pipeline: Data processing pipeline components
    - utils: Utility functions for logging and caching
    - config: Global configuration and logging setup

Example:
    >>> from traffic_sim_module.pipeline.main_pipeline import run_pipeline
    >>> run_pipeline(xml_path="data.xml", output_dir="output/")
"""

__version__ = "0.0.1"
